# Zyra benchmarks

This repository currently contains a CI benchmark for Hical, modeled after the
merjs benchmark workflow style: build, measure binary size, run `wrk`, sample
RSS memory, post a GitHub Actions summary, and upload a JSON artifact.

Run it from **Actions → Benchmark — Hical** or push to `main`.
